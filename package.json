{
  "name": "nanobeacon",
  "description": "Small navigator.sendBeacon wrapper",
  "repository": "yoshuawuyts/nanobeacon",
  "version": "1.0.4",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "bankai start example",
    "test": "standard && npm run deps"
  },
  "dependencies": {},
  "devDependencies": {
    "bankai": "^7.6.2",
    "dependency-check": "^2.8.0",
    "standard": "^10.0.2",
    "tape": "^4.6.3"
  }
}
